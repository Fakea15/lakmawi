from django.contrib import admin
from .models import Post, Message, Profile

# models as data center
admin.site.register(Post)
admin.site.register(Message)
admin.site.register(Profile)
from django.contrib import admin

# Register your models here.
